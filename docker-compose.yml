services:
  backend:
    build:
      context: ./LABMM
    env_file: .env
    volumes:
      - sqlite_data:/app/instance
    restart: unless-stopped

  frontend:
    build:
      context: ./LABMMIF
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  sqlite_data:
